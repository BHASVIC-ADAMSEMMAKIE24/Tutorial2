
# global variable
# must be declared before its use
age = 8


# Parameter
# Passing a global variable into a function uses the value of the variable but does not permanantly change it
def function(age):
    #Local variable as it is within the function
    age = 8

#returning the age allows it to be used outside the function
return age








